@page "/login"
@using VotingSystem.API.DTO.Responses

<PageTitle>Login</PageTitle>

@code {
    public LoginResponse AccountLogin(string username, string password)
    {

        return new LoginResponse()
        {
            UserId = 1,
            AccessToken = "what",
            ExpiresIn = 23,
        };
    }
}